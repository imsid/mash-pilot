"""Personal agents — the product-brief archetypes.

`morning_brief` is a personal agent over a cloud service (GitHub, via MCP);
`finance_watch` is a personal agent over your own local data. Both register
unconditionally so the catalog is always browsable; capabilities light up
when configuration (credentials, data) exists.
"""
